# AI Voice Agent — Landing Page

This is a minimal Vite + React + Tailwind scaffold for a SaaS landing page showcasing an AI Voice Agent for phone calls.

Quick start:

```bash
cd productpage
npm install
npm run dev
```

Open http://localhost:5173
